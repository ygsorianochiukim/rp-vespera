import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  output: 'export',
  // basePath: '/lsp-next',
  // assetPrefix: '/lsp-next/',
};

export default nextConfig;
